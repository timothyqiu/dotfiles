[project-build]
command=cmake --build build -j2
output=quickfix
save=2

[project-run]
command=cmake --build build -j2 && build/src/$(VIM_DIRNAME)
output=terminal
save=2

[project-debug]
command=cmake --build build -j2 && lldb build/src/$(VIM_DIRNAME)
output=terminal
pos=bang
save=2
